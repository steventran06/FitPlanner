<%= form_for(@schedule) do |f| %>
  <% if @schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>
      <ul>
      <% @schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li></ul>
      <% end %>
    </div>
  <% end %>

  <p>Day of the week:</p>
  <div>
    <%= f.select :day, [['Sunday'], ['Monday'], ['Tuesday'], ['Wednesday'], ['Thursday'], ['Friday'], ['Saturday']] %>
  </div>
  <br>
  <p>Muscle Group:</p>
  <div>
    <%= f.select :muscle_group, [['Abdominals'], ['Hamstrings'], ['Adductors'], ['Quadriceps'], ['Biceps'], ['Shoulders'], ['Chest'], ['Middle Back'], ['Lower Back'], ['Calves'], ['Glutes'], ['Lats'], ['Triceps'], ['Traps'], ['Forearms'], ['Neck']] %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
